version: 2
models:
  - name: stg_orders
    columns:
      - name: order_key
        tests:
          - unique
          - not_null
      - name: total_price
        tests:
          - dbt_utils.expression_is_true:
              arguments:    
                expression: ">= 0"